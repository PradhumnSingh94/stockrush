apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: stockrush
  labels:
    monitor: "true"
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 3004
      targetPort: 3004
  type: ClusterIP