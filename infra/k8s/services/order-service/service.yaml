apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: stockrush
  labels:
    monitor: "true"
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 3002
      targetPort: 3002
  type: ClusterIP