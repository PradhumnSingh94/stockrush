apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: stockrush
  labels:
    monitor: "true"
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 3003
      targetPort: 3003
  type: ClusterIP